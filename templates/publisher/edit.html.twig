{% extends 'base.html.twig' %}

{% block title %}Edit Publisher #{{ publisher.id | escape }}{% endblock %}

{% block body %}
    <h1>Edit Publisher #{{ publisher.id | escape }}: "{{ publisher.name | escape }}"</h1>
    {{ form(form) }}
{% endblock %}
