{% extends 'base.html.twig' %}

{% block title %}Edit Book #{{ book.id | escape }}{% endblock %}

{% block body %}
    <h1>Edit Book #{{ book.id | escape }}: "{{ book.name | escape }}"</h1>
    {{ form(form) }}
{% endblock %}
